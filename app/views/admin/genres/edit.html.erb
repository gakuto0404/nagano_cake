<div class="container">
  <div class="alert-secondary">ジャンル編集</div>
  <div class="row">
   <div class="col-md-4">
    <%= form_with @genre do |f| %>
      <h3><%= f.text_field :name %></h3>
      <%= f.submit '更新', class: "btn btn-primary" %>
      <p></p>
    <% end %>
    </div>
  </div>
</div>