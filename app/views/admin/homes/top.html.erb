<div class="container">
  <div class="alert-secondary">注文履歴一覧</div>
  <div class="row">
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th scope="col">購入日時</th>
          <th scope="col">購入者</th>
          <th scope="col">注文個数</th>
        </tr>
      </thead>
      <% @order_details.each do |order_detail| %>
        <tbody>
          <tr>
            <td><%= link_to order_detail.created_at %></td>   <!-- 購入日時のちに注文履歴詳細リンクにする -->
            <td><%= order_detail.name %></td>  <!-- 購入者名 -->
            <td><%= order_detail.amount %></td>  <!-- 注文個数 -->
          </tr>
        </tbody>
      <% end %>
      <%= paginate @os, class: "paginate" %>  <!-- ページネーション -->
    </table>
  </div>
</div>
