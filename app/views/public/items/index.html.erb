<div class="container">
  <div class="row">
    <div class="col-md-3 mt-4">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th scope="col">ジャンル検索</th>
          </tr>
        </thead>
        <% @genres.each do |genre| %>
          <tbody>
            <tr>
              <td><%= genre.name %></td>
            </tr>
          </tbody>
        <% end %>
      </table>
    </div>
    <section class="new-items col-md-8 mt-4">
      <h2 class="new-items-title"><strong>商品一覧</strong></h2>
      <div class="row">
        <% @items.each do |item| %>
          <div class="col-md-3 mt-4">
            <div class="card mb-4 shadow-sm">
              <%= image_tag item.image %>
                <div class="card-body">
                  <%= item.name %><br>
                  ￥<%= item.price %>
                </div>
            </div>
          </div>
        <% end %>
      </div>
    </section>
  </div>
</div>
